[package]
name = "worker"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
tokio = { version = "1", features = ["full"] }
domain = { path = "../domain" }
queue = { path = "../queue" }
rpc-client = { path = "../rpc-client" }
dotenvy = "0.15"
async-trait = "0.1"
serde_json = "1"
